restclient:
  ssl:
    forall: false
    enabled: false
    cncheck: true
    truststore:
      file: some/file
      password: random
    keystore:
      file:
      password: random2
      type: ASDF
      alias: "2"
    supportedprotocols: TLSv1.2, TLSv1
    supportedciphersuites:
